<Window x:Class="Software_Projekt.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Software_Projekt"
        mc:Ignorable="d"
        Title="Statistik-Trainer" Height="450" Width="800" MinHeight="400" MinWidth="700" Background="Gray" Tag="MainWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel>
            <TextBlock  Text="Statistik-Trainer" FontSize="50" HorizontalAlignment="Center" Margin="20"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Button Style="{StaticResource ResourceKey=ButtonStyle}" Content="Analysieren" Click="OnAnalyseFile"/>
            <Button Style="{StaticResource ResourceKey=ButtonStyle}" Content="Kennzahlen" Click="OnOpenIndicators"/>
            <Button Style="{StaticResource ResourceKey=ButtonStyle}" Content="Verlassen" Click="OnClickEnd"/>
        </StackPanel>
    </Grid>
</Window>
